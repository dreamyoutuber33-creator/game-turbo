<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_amoled"
    android:fitsSystemWindows="true">

    <!-- Top Gaming Header Bar -->
    <LinearLayout
        android:id="@+id/top_gaming_header"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:background="@color/bg_amoled"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_top_app_logo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="Game Turbo Logo"
            android:src="@drawable/ic_speed"
            app:tint="@color/accent_cyan" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_top_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-black"
                android:letterSpacing="0.08"
                android:text="GAME TURBO PRO"
                android:textColor="@color/accent_cyan"
                android:textSize="17sp" />

            <TextView
                android:id="@+id/tv_top_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hardware Engine • AMOLED Dark"
                android:textColor="@color/text_dim"
                android:textSize="10sp" />
        </LinearLayout>

        <!-- Quick Accent Switcher -->
        <ImageView
            android:id="@+id/btn_quick_accent"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Accent Theme"
            android:padding="8dp"
            android:src="@drawable/ic_palette"
            app:tint="@color/accent_cyan" />

        <!-- Quick Boost Status Chip -->
        <TextView
            android:id="@+id/btn_quick_boost_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:background="@drawable/bg_badge_gaming"
            android:clickable="true"
            android:focusable="true"
            android:fontFamily="sans-serif-black"
            android:foreground="?attr/selectableItemBackground"
            android:letterSpacing="0.05"
            android:paddingHorizontal="10dp"
            android:paddingVertical="5dp"
            android:text="⚡ BOOST"
            android:textColor="@color/accent_lime"
            android:textSize="11sp" />
    </LinearLayout>

    <View
        android:id="@+id/top_divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/border_subtle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/top_gaming_header" />

    <!-- Fragment / Tab View Container -->
    <FrameLayout
        android:id="@+id/fl_content_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/bottom_divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/top_divider" />

    <View
        android:id="@+id/bottom_divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/border_subtle"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="64dp"
        android:background="@color/bg_nav_bar"
        app:elevation="8dp"
        app:itemIconSize="22dp"
        app:itemIconTint="@color/nav_item_selector"
        app:itemRippleColor="@color/glow_cyan"
        app:itemTextColor="@color/nav_item_selector"
        app:labelVisibilityMode="labeled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:menu="@menu/bottom_nav_menu" />

</androidx.constraintlayout.widget.ConstraintLayout>
